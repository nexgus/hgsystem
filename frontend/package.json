{
  "name": "hgsystem-frontend",
  "private": true,
  "version": "0.7.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:dev": "vue-tsc -b && vite build --minify false --mode development",
    "build": "vue-tsc -b && vite build --mode production",
    "preview": "vite preview"
  },
  "dependencies": {
    "@wailsio/runtime": "latest",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.0",
    "typescript": "^5.6.0",
    "vite": "^8.0.5",
    "vue-tsc": "^2.1.0"
  }
}
