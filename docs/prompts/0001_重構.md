重新整理程式 (包含檔案架構), 將 GUI 與邏輯分離. 有什麼問題可以先問我.

-----
1. (B) 把這次重構當成 source/hgsys/ 的起點, 一次到位寫成新版
2. (C) MVVM 風格
3. 修正 deprecated/customer.py:174-175 與 deprecated/customer.py:309, 其他先不用修改.
4. (B) 做成 Python package, 主程式寫在 __main__.py 內, 並在 project 的根目錄寫一個 pyproject.toml. 把 requirements.txt 更新為開發時必須安裝的必要套件 (如果不需要就把它刪除)
