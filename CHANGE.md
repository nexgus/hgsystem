# 變更紀錄 (CHANGE LOG)

本檔案記錄各版本相對前一版的差異.

## 0.8.0
### 新功能
- 新增「檢查更新…」功能

### 改變
- 「關於」改以獨立視窗顯示, 含「關於」與「第三方授權」兩個分頁.
- 主選單改依 macOS 與 Windows 各自的慣例重整

### 修正
- (無)

## 0.7.0
0.7.0 為整套系統自原本的 Python + PySide 重寫為 Go + Wails 3 + Vue 3 + TypeScript 之版本. 以下差異皆相對於改寫前的 PySide 版本.

### 新功能
- 搜尋視窗可指定 "收件期間" 或 "交件期間", 以日期區間搜尋客戶.
- 可搜尋無年份的生日 (僅輸入月與日), 結果依生日先後排序, 便於製作壽星名單.
- 歷史工單表與搜尋結果表取得焦點後, 可用 Up / Down / PgUp / PgDown / Home / End 鍵移動; 搜尋結果表並可按 Enter 確認選取.
- 稱謂改為可自行維護的清單, 能新增與刪除稱謂; 修改到清單以外的舊稱謂時, 會提示是否將其加入清單.
- 日期欄位旁新增日曆按鈕, 可改用系統日期選擇器挑選日期.
- 介面自動跟隨系統的淺色或深色佈景主題.
- 備份與還原對話框會記住上次選取的目錄, 下次開啟時自動定位至該位置; 備份與還原各自獨立記憶, 設定存於使用者設定目錄下的 hgsystem/settings.json.

### 改變
- 整套系統自 Python + PySide 重寫為 Go + Wails 3 + Vue 3 + TypeScript, 改為單一執行檔.
- 稱謂輸入由文字框改為下拉式選單.
- 日期輸入由 "民國年 + 月下拉 + 日下拉" 三段式, 改為單一文字框, 支援多種格式 (如 114/05/29, 5/29, 1971/05/29, 1140529).
- 歷史工單改為依收件日由新到舊排列 (最新者在最上方); 舊版則依新增順序排列, 最新者在最下方.
- 移除所有 Alt 加字母的按鍵快捷鍵.
- 輸入框改為非 3D 之扁平樣式, 以免於深色背景下不易辨識.
- 對齊左上角 (客戶資料) 與右上角 (歷史工單) 兩區塊的高度.
- 移除應用程式內建的自我更新功能.
- 備份與還原改為優先使用主機上的 mongodump / mongorestore; 主機未安裝 MongoDB Database Tools 時, 自動改用 MongoDB 容器內的工具 (透過 docker exec 搭配 docker cp 在容器與主機間傳遞 dump).

### 修正
- (無)
